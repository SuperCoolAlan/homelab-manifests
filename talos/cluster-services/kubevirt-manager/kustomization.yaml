apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubevirt-manager

# bundled.yaml creates the namespace + RBAC + deployment + service + CRDs.
# https://github.com/kubevirt-manager/kubevirt-manager/releases
resources:
  - https://raw.githubusercontent.com/kubevirt-manager/kubevirt-manager/v1.5.4/kubernetes/bundled.yaml
  - ingress.yaml
