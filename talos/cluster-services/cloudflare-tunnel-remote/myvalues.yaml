# Tunnel token is supplied via a SOPS-encrypted Secret (name: cloudflare-tunnel-remote, key: tunnelToken).
# The chart's built-in secret template was removed so we can provide our own.
cloudflare:
  tunnel_token: ""

# Pin to a specific cloudflared release.
# https://github.com/cloudflare/cloudflared/releases
image:
  # renovate: datasource=docker depName=cloudflare/cloudflared
  tag: "2026.9.1"

fullnameOverride: cloudflare-tunnel-remote

replicaCount: 2

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi
