apiVersion: kubevirt.io/v1
kind: KubeVirt
metadata:
  name: kubevirt
  namespace: kubevirt
spec:
  infra:
    nodePlacement:
      nodeSelector:
        kubernetes.io/hostname: talos-ramhaus
  configuration:
    developerConfiguration:
      useEmulation: false
