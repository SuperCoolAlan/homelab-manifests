apiVersion: v1
kind: Namespace
metadata:
  name: nfs-csi
  labels:
    # democratic-csi node/controller pods need privileged (hostPath mounts,
    # SYS_ADMIN, host namespaces) to do the NFS mounting on nodes.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: restricted
