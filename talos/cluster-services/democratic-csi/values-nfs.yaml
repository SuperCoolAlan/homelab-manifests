csiDriver:
  name: "nfs"
storageClasses:
- name: truenas-nfs
  # No longer the cluster default (2026-08-09). Any PVC created without an
  # explicit storageClassName used to land on TrueNAS - the machine being
  # decommissioned. Default moved to fast-array (local ZFS mirrors on ramhaus).
  # This class stays declared until no PVs reference it and democratic-csi is
  # retired.
  defaultClass: false
  reclaimPolicy: Delete
  volumeBindingMode: Immediate
  allowVolumeExpansion: true
  parameters:
    fsType: nfs
    detachedVolumesFromSnapshots: "false"
  mountOptions:
  - noatime
  - nfsvers=4
  secrets:
    provisioner-secret:
    controller-publish-secret:
    node-stage-secret:
    node-publish-secret:
    controller-expand-secret:
volumeSnapshotClasses:
- name: truenas-nfs
  parameters:
    detachedSnapshots: "true"

controller:
  driver:
    image:
      tag: next
      pullPolicy: Always
  externalResizer:
    enabled: true

node:
  driver:
    image:
      tag: next
      pullPolicy: Always

# NOTE: Driver config is stored in secrets/driver-config.enc.yaml (SOPS encrypted)
# The existingConfigSecret below overrides the config section - changes here have no effect
driver:
  existingConfigSecret: nfs-csi-democratic-csi-driver-config
  config:  # NOT USED - kept for reference only
    driver: freenas-api-nfs
    instance_id:
    httpConnection:
      protocol: http
      host: truenas.asandov.local
      port: 80
      apiVersion: 2
      #apiKey: get your own key
      allowInsecure: true
    zfs:
      datasetParentName: ST6000NM0095-pool1/media/nfs/v
      detachedSnapshotsDatasetParentName: ST6000NM0095-pool1/media/nfs/s
      datasetEnableQuotas: true
      datasetEnableReservation: false
      datasetPermissionsMode: "0775"
      datasetPermissionsUser: 1000
      datasetPermissionsGroup: 3001
    nfs:
      shareHost: truenas.asandov.local
      shareAlldirs: false
      shareAllowedHosts: []
      shareAllowedNetworks: []
      shareMaprootUser: truenas_admin
      shareMaprootGroup: truenas_admin
      shareMapallUser: ""
      shareMapallGroup: ""

