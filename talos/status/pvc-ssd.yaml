# Rollback copy from before the 2026-09-15 move to gatus-data-fast; drop it after the soak.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gatus-data-ssd
  namespace: status
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ssd-array
  resources:
    requests:
      storage: 1Gi
