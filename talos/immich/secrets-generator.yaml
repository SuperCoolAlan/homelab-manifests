apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: immich-secret-generator
  annotations:
    config.kubernetes.io/function: |
        exec:
          path: ksops
files:
  - secrets/b2-asandov-cnpg-credentials.enc.yaml
  - secrets/immich-config.enc.yaml
  # Uncomment once immich-photos-restic.enc.yaml exists (see the PLAINTEXT stub
  # in secrets/). Listing a missing file here breaks the whole app's render.
  # - secrets/immich-photos-restic.enc.yaml
