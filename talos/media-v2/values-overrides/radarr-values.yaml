# Disabled - exportarr needs secret "radarr" with key "apiKey"
metrics:
  enabled: false

# Inherited from original media deployment
persistentVolumeClaims:
  radarr-config:
    requestStorage: '10Gi'
    storageClassName: 'nfs'
  # Replacement on the local ZFS `fast` pool. storageClassName is immutable on
  # a PVC, hence a second claim rather than editing the first. The original is
  # kept while the migration soaks so rollback is a one-line claimName change -
  # its PV is Retain and still holds the data.
  radarr-config-fast:
    requestStorage: '10Gi'
    storageClassName: 'fast-array'

# Disable config.xml mounting - let app manage its own config
application:
  config: null

ingress:
  enabled: true
  className: traefik
  host: radarr.asandov.local
  path: /
  pathType: Prefix
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: authentik-authentik@kubernetescrd

# Configure deployment with volumes and API key sync sidecar
deployment:
  nodeSelector:
    kubernetes.io/hostname: talos-ramhaus
  serviceAccountName: media-apps
  # Configure volumes to use shared PVCs
  volumes:
    config:
      persistentVolumeClaim:
        claimName: 'radarr-config'
    sabnzbd-downloads:
      persistentVolumeClaim:
        claimName: 'sabnzbd-downloads'
    media-library:
      persistentVolumeClaim:
        claimName: 'media-library'
  # Configure volume mounts with subPath
  container:
    resources:
      requests:
        memory: "512Mi"
        cpu: "250m"
      limits:
        memory: "4Gi"
        cpu: "2000m"
    volumeMounts:
      - name: 'config'
        mountPath: '/config'
      - name: 'sabnzbd-downloads'
        mountPath: '/downloads-usenet'
      # Single full mount so imports from /media/torrents into /media/film
      # can hardlink (same filesystem AND same mount). Root folder path
      # /media/film is unchanged.
      - name: 'media-library'
        mountPath: '/media'