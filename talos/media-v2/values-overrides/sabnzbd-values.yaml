# Inherited from original media deployment
persistentVolumeClaims:
  sabnzbd-config:
    requestStorage: '5Gi'
    storageClassName: 'nfs'

deployment:
  volumes:
    downloads:
      persistentVolumeClaim:
        claimName: 'media-downloads'

ingress:
  enabled: true
  className: nginx
  host: sabnzbd.asandov.local
  path: /
  pathType: Prefix
  annotations:
    external-dns.alpha.kubernetes.io/hostname: sabnzbd.asandov.local

# VPN configuration from original deployment
# Note: May need adjustment based on chart support
# vpn:
#   enabled: true
#   provider: windscribe
#   type: openvpn
#   existingSecret: windscribe-openvpn-creds