# TEMPORARY quiesce for the fast->ssd-array PVC migration (2026-08-12).
# Removed in the same commit that repoints the claim to the -ssd volume.
# Explicit namespace: kustomize hard-fails the whole render without it.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prowlarr
  namespace: media-v2
spec:
  replicas: 0
