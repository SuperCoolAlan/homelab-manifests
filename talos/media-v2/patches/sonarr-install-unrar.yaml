- op: add
  path: /spec/template/spec/containers/0/lifecycle
  value:
    postStart:
      exec:
        command:
        - /bin/sh
        - -c
        - apk update > /dev/null 2>&1 && apk add --no-cache 7zip > /dev/null 2>&1 || true
