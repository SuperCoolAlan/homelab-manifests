apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tpot

resources:
  - namespace.yaml
  - datavolume.yaml
  - virtualmachine.yaml
  - service.yaml
  - ingress.yaml
  - networkpolicy.yaml

generators:
  - secrets-generator.yaml
