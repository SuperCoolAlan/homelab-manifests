apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: maintainerr

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - pvc-fast.yaml
  - pvc-ssd.yaml
  - ingress.yaml

patches:
  - target:
      kind: Deployment
      name: maintainerr
    path: quiesce.yaml
