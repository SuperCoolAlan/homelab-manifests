# Rollback copy from before the 2026-09-15 move to maintainerr-data-fast; drop it after the soak.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maintainerr-data-ssd
  namespace: maintainerr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ssd-array
  resources:
    requests:
      storage: 1Gi
