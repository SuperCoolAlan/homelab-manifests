# Rollback copy from before the 2026-09-15 move to qbittorrent-config-fast; drop it after the soak.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-config-ssd
  namespace: qbittorrent
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ssd-array
  resources:
    requests:
      storage: 5Gi
