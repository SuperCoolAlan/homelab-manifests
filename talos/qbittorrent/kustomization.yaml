apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: qbittorrent

resources:
  - namespace.yaml
  - pvcs.yaml
  - pvc-fast.yaml
  - pvc-ssd.yaml
  - local-asandov-ingress.yaml

generators:
  - secrets-generator.yaml

patches:
  - target:
      kind: Deployment
      name: qbittorrent
    path: quiesce.yaml
  - target:
      kind: Deployment
      name: qbittorrent
    path: patches/gluetun-secrets-dns.yaml

helmCharts:
  - name: qbittorrent
    repo: https://charts.alekc.dev
    version: 2.7.5
    releaseName: qbittorrent
    namespace: qbittorrent
    valuesFile: values.yaml
