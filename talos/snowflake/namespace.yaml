# Privileged only because wg-up needs NET_ADMIN; the forwarder itself stays unprivileged.
apiVersion: v1
kind: Namespace
metadata:
  name: snowflake
  labels:
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged
