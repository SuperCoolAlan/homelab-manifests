apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: snowflake

resources:
  - namespace.yaml
  - networkpolicy.yaml
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
  - vmrule.yaml

generators:
  - secrets-generator.yaml
