apiVersion: v1
kind: Service
metadata:
  name: snowflake
  namespace: snowflake
  labels:
    app.kubernetes.io/name: snowflake
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: snowflake
  ports:
    - name: metrics
      port: 9999
      targetPort: metrics
      protocol: TCP
