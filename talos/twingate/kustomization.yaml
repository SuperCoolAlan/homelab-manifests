apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: twingate

generators:
  - secrets-generator.yaml

helmGlobals:
  chartHome: ./helm

helmCharts:
  - name: connector
    releaseName: twingate-op
    namespace: twingate
