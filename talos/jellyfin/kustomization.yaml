apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jellyfin

# NOTE: this directory is NOT in the talos-apps ApplicationSet allowlist
# (talos/argocd/resources/applicationset.yaml), so none of it is deployed.
# See README.md for how to enable and what must be true first.
resources:
  - namespace.yaml
  - pvc.yaml
  - media-pv.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
