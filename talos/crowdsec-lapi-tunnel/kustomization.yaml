apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: crowdsec-lapi-tunnel

resources:
  - namespace.yaml
  - tunnels.yaml
  - networkpolicy.yaml

generators:
  - secrets-generator.yaml
