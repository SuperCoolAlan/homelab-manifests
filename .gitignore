talos/config/*
!talos/config/*.enc.yaml
.decrypted*
**/.env
config.yaml
**/out.yaml
!talos/media/secrets/*.enc.yaml

# GPG/SOPS secrets - never commit these
*.asc
sops-gpg*
*-secret.yaml

*.key
*.asc
*.gpg
!**/secrets/*.enc.yaml
!**/secrets/*-generator.yaml

# Kustomize helm chart cache - charts are referenced from remote repos
**/charts/
talos/cluster-services/traefik/helm/

# Postgres migration dumps - contain user data, never commit (repo is public)
talos/immich/*.dump
talos/immich/*.sql
